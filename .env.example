# ===== Palmeiras Hub =====
# Nenhuma chave de API de futebol é necessária.
# O app busca dados públicos no browser a cada abertura.

# Porta do proxy Express OPCIONAL (npm run dev:proxy / npm start)
PORT=3001

# (Removido) API_FOOTBALL_KEY / FOOTBALL_DATA_API_KEY / FORCE_DEMO
# — não use plano free pago/limitado; fontes públicas no client.
